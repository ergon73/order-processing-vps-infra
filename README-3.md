# Order Processing — VPS Infrastructure

Учебный проект: веб-приложение для обработки заявок на услуги с админ-панелью, JWT-авторизацией, системой сбора поведенческих метрик и интеллектуальной приоритизацией заявок.

**Стек:** FastAPI + PostgreSQL + Nginx + Docker Compose  
**VPS:** service.prompt-engineer.su (194.67.205.158)

---

## 📋 Возможности

### Клиентская часть (Главная страница)
- Форма заявки с выбором услуги из динамического списка
- Сбор поведенческих метрик в реальном времени:
  - Время на странице (секунды)
  - Клики по элементам интерфейса
  - Координаты курсора для построения heatmap
- Отправка метрик на сервер каждую секунду

### Админ-панель
- **JWT-авторизация** — безопасный вход через логин/пароль
- **Управление услугами (CRUD):**
  - Создание услуг через визуальную таблицу
  - Редактирование существующих услуг
  - Удаление услуг с автоматическим обновлением на главной странице
- **Просмотр заявок с приоритизацией:**
  - Автоматическое ранжирование по «температуре» лида
  - Визуальные индикаторы приоритета (красный/желтый/зеленый)
  - Детальная карточка каждой заявки
- **Статистика пользователей:**
  - Среднее время на странице (день/неделя/месяц)
  - Heatmap популярных координат курсора
  - Анализ активности пользователей

### Безопасность и деплой
- **Production-ready конфигурация:**
  - Доступ к сайту только через HTTPS (порт 443)
  - Backend и PostgreSQL недоступны снаружи (внутренняя docker-сеть)
  - Закрытие прямых портов 8000 и 5432 (HTTP 403)
- **Изоляция сервисов:**
  - pgAdmin и docker registry отключены в production
  - Минимальная поверхность атаки

---

## 🏗️ Архитектура

```
                  Internet
                     |
                     v
              Nginx (443/80)
                     |
      +--------------+---------------+
      |                              |
      v                              v
Static Frontend              FastAPI Backend (8000)
(HTML/CSS/JS)                       |
                                    v
                              PostgreSQL (5432)
```

**Принципы:**
- Nginx — единственная точка входа (reverse proxy)
- Backend и база данных изолированы во внутренней docker-сети
- Все взаимодействие через REST API

---

## 🚀 Быстрый старт

### Требования
- **OS:** Ubuntu 22.04 (или совместимая)
- **Docker:** версия 20.10+
- **Docker Compose:** версия 2.0+

### 1. Клонирование репозитория

```bash
git clone https://github.com/ergon73/order-processing-vps-infra.git
cd order-processing-vps-infra
```

### 2. Настройка переменных окружения

Создайте файл `.env` на основе примера:

```bash
cp .env.example .env
```

Отредактируйте `.env` и установите следующие переменные:

```env
# FastAPI
SECRET_KEY=<ваш_секретный_ключ_минимум_32_символа>
DATABASE_URL=postgresql://orderuser:orderpass@postgres:5432/orderdb

# PostgreSQL
POSTGRES_USER=orderuser
POSTGRES_PASSWORD=orderpass
POSTGRES_DB=orderdb
```

**Генерация SECRET_KEY:**
```bash
openssl rand -hex 32
```

### 3. Генерация SSL сертификата (для HTTPS)

Для разработки создайте самоподписанный сертификат:

```bash
mkdir -p ssl
openssl req -x509 -nodes -days 365 -newkey rsa:2048 \
  -keyout ssl/key.pem \
  -out ssl/cert.pem \
  -subj "/CN=service.prompt-engineer.su"
```

> Для продакшена используйте Let's Encrypt или настоящий SSL-сертификат.

### 4. Сборка и запуск

```bash
# Сборка и запуск всех контейнеров в фоне
sudo docker compose up --build -d

# Проверка статуса контейнеров
sudo docker compose ps

# Просмотр логов backend
sudo docker logs -f backend
```

### 5. Проверка работоспособности

- **Главная страница:** https://service.prompt-engineer.su/
- **Админ-панель:** https://service.prompt-engineer.su/admin.html
- **Swagger UI (dev):** http://194.67.205.158:8000/docs (если порт 8000 открыт)

> **Примечание:** При первом посещении браузер может показать предупреждение о безопасности (из-за самоподписанного сертификата). Нажмите "Advanced" → "Proceed" для продолжения.

---

## 📂 Структура проекта

```
order-processing-vps-infra/
├── backend/                  # FastAPI приложение
│   ├── app/
│   │   ├── main.py          # Точка входа
│   │   ├── database.py      # Подключение к БД
│   │   ├── models.py        # SQLAlchemy модели
│   │   ├── schemas.py       # Pydantic схемы
│   │   └── routers/         # API эндпоинты
│   │       ├── auth.py      # JWT авторизация
│   │       ├── admin_settings.py   # CRUD услуг
│   │       ├── applications.py     # Заявки
│   │       └── behavior_metrics.py # Метрики
│   ├── requirements.txt     # Python зависимости
│   └── Dockerfile
├── frontend/                 # Статические файлы
│   ├── src/
│   │   ├── index.html       # Главная страница
│   │   ├── admin.html       # Админ-панель
│   │   ├── js/
│   │   │   ├── admin.js
│   │   │   ├── behavior-metrics.js
│   │   │   └── auth.js
│   │   └── css/
│   │       └── styles.css
│   ├── package.json
│   └── webpack.config.js    # Сборка фронтенда
├── nginx/
│   └── conf.d/
│       └── default.conf     # Конфигурация Nginx
├── scripts/
│   └── seed_applications.sql  # Тестовые данные
├── ssl/                      # SSL сертификаты
│   ├── cert.pem
│   └── key.pem
├── docker-compose.yml        # Оркестрация контейнеров
├── .env                      # Переменные окружения (не в git!)
├── .env.example              # Пример .env файла
├── .gitignore
└── README.md                 # Этот файл
```

---

## 🔌 API Endpoints

### Авторизация (`/api/auth/`)
- `GET /check` — Проверка наличия админов в системе
- `POST /register` — Регистрация первого админа
- `POST /login` — Вход (получение JWT токена)
- `GET /me` — Получение профиля текущего админа (требует JWT)

### Услуги (`/api/admin-settings/`)
- `GET /` — Список всех услуг
- `POST /` — Создание новой услуги (требует JWT)
- `PUT /{id}` — Обновление услуги (требует JWT)
- `DELETE /{id}` — Удаление услуги (требует JWT)

### Заявки (`/api/applications/`)
- `GET /` — Список заявок с приоритизацией (требует JWT)
- `POST /` — Создание новой заявки (публичный)
- `GET /{id}` — Детали конкретной заявки (требует JWT)

### Метрики (`/api/behavior-metrics/`)
- `POST /` — Отправка поведенческих метрик (публичный)
- `GET /stats` — Получение статистики (требует JWT)

**Документация API:** http://194.67.205.158:8000/docs (Swagger UI, доступен в dev-режиме)

---

## 🗄️ База данных

### Таблицы

#### `admins`
Хранит данные администраторов.
```sql
CREATE TABLE admins (
    id SERIAL PRIMARY KEY,
    email VARCHAR(255) UNIQUE NOT NULL,
    hashed_password VARCHAR(255) NOT NULL,
    created_at TIMESTAMP DEFAULT NOW()
);
```

#### `admin_settings` (или `services`)
Список доступных услуг.
```sql
CREATE TABLE admin_settings (
    id SERIAL PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    description TEXT,
    created_at TIMESTAMP DEFAULT NOW()
);
```

#### `applications`
Заявки пользователей.
```sql
CREATE TABLE applications (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    surname VARCHAR(100) NOT NULL,
    patronymic VARCHAR(100),
    email VARCHAR(255) NOT NULL,
    phone VARCHAR(20),
    service_id INTEGER REFERENCES admin_settings(id),
    business_info TEXT,
    business_niche VARCHAR(100),
    company_size VARCHAR(50),
    budget VARCHAR(100),
    deadline VARCHAR(100),
    convenient_time VARCHAR(100),
    preferred_contact_method VARCHAR(50),
    additional_comments TEXT,
    priority_score INTEGER DEFAULT 0,
    created_at TIMESTAMP DEFAULT NOW()
);
```

#### `behavior_metrics`
Поведенческие метрики пользователей.
```sql
CREATE TABLE behavior_metrics (
    id SERIAL PRIMARY KEY,
    application_id INTEGER,  -- может быть NULL для анонимных метрик
    time_on_page INTEGER,
    buttons_clicked TEXT,  -- JSON строка
    cursor_positions TEXT,  -- JSON строка
    return_frequency INTEGER,
    created_at TIMESTAMP DEFAULT NOW()
);
```

### Инициализация тестовых данных

Для проверки приоритизации заявок выполните SQL скрипт:

```bash
# Из консоли хоста
sudo docker exec -i postgres psql -U orderuser -d orderdb < scripts/seed_applications.sql

# Или через pgAdmin (если включен в dev-режиме)
# Query Tool → открыть файл scripts/seed_applications.sql → Execute
```

---

## 🎯 Система приоритизации заявок

### Алгоритм

Каждая заявка получает **score** (оценку) на основе трех критериев:

1. **Бюджет (40% веса)**
   - 5M+ рублей → 40 баллов
   - 1-5M → 30 баллов
   - 500k-1M → 20 баллов
   - <500k → 10 баллов

2. **Размер компании (30% веса)**
   - 500+ сотрудников → 30 баллов
   - 100-500 → 20 баллов
   - 50-100 → 15 баллов
   - <50 → 5 баллов

3. **Срочность (30% веса)**
   - Срок "1 неделя" или в комментариях "срочно" → 30 баллов
   - Срок "1 месяц" → 15 баллов
   - Срок "3+ месяца" → 5 баллов

### Визуализация приоритета

В админ-панели заявки отображаются с цветовыми индикаторами:

- 🔥 **Красный** (score > 80) — Высокий приоритет (Urgent)
- ⚠️ **Желтый** (score 50-80) — Средний приоритет (Medium)
- ✅ **Зеленый** (score < 50) — Низкий приоритет (Low)

Список автоматически сортируется по убыванию score.

---

## 📊 Поведенческие метрики

### Сбор данных

На главной странице в реальном времени собираются:
- **Время на странице** — счетчик в секундах
- **Клики по элементам** — объект `{element_id: count}`
- **Координаты курсора** — массив точек `[{x, y}, ...]`

Данные отправляются на сервер каждую секунду через `POST /api/behavior-metrics/`.

### Анализ в админ-панели

Раздел "Статистика пользователей" показывает:

1. **Среднее время на странице:**
   - За последний день
   - За последнюю неделю
   - За последний месяц

2. **Heatmap (тепловая карта):**
   - Визуализация популярных зон страницы
   - Отображение через Canvas API или Chart.js
   - Градиент от синего (холодный) к красному (горячий)

**Использование:** Оптимизация UX на основе реального поведения пользователей.

---

## 🔒 Безопасность

### Production конфигурация

В файле `docker-compose.yml` для продакшена:

```yaml
services:
  nginx:
    ports:
      - "443:443"  # Только HTTPS наружу
      - "80:80"    # Опционально для редиректа на HTTPS
  
  backend:
    # БЕЗ секции ports: — доступен только изнутри docker-сети
    expose:
      - "8000"
  
  postgres:
    # БЕЗ секции ports: — доступен только изнутри docker-сети
```

### Проверка безопасности

```bash
# Попытка подключения к backend (должен вернуть 403 или connection refused)
curl -i http://194.67.205.158:8000/

# Попытка подключения к PostgreSQL (должен вернуть 403 или connection refused)
curl -i http://194.67.205.158:5432/

# Проверка HTTPS (должен вернуть 200 OK)
curl -kI https://service.prompt-engineer.su/
```

### JWT авторизация

- Токены генерируются при логине (`POST /api/auth/login`)
- Хранятся в `localStorage` на клиенте
- Передаются в заголовке: `Authorization: Bearer <token>`
- Срок жизни: настраивается в backend (обычно 24 часа)

---

## 🛠️ Разработка

### Режим разработки

Для удобства разработки можно включить дополнительные сервисы:

**docker-compose.yml:**
```yaml
services:
  # ... основные сервисы

  pgadmin:
    profiles: ["dev"]  # Не запускается по умолчанию
    image: dpage/pgadmin4
    ports:
      - "5050:80"
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@admin.com
      PGADMIN_DEFAULT_PASSWORD: admin
```

**Запуск в dev-режиме:**
```bash
sudo docker compose --profile dev up --build -d
```

### Работа с двумя терминалами

Для эффективной разработки используйте два терминала:

**Терминал A (Docker):**
```bash
cd ~/order-processing-vps-infra
sudo docker compose up --build -d backend
sudo docker logs -f backend
```

**Терминал B (Frontend):**
```bash
cd ~/order-processing-vps-infra/frontend
npm run build
# После изменений в JS/CSS пересобирайте
```

### Сборка фронтенда

```bash
cd frontend
npm install          # Первый раз
npm run build        # Сборка
npm run dev          # Dev-сервер (опционально)
```

Результат сборки попадает в `frontend/dist/`, откуда Nginx его отдает.

---

## 🐛 Troubleshooting

### Проблема: Контейнеры не запускаются

**Решение:**
```bash
# Проверьте логи
sudo docker compose logs

# Проверьте статус
sudo docker compose ps

# Пересоберите с нуля
sudo docker compose down -v
sudo docker compose up --build -d
```

### Проблема: Swagger UI не открывается (404)

**Возможные причины:**
- Nginx неправильно проксирует `/api/docs`
- Backend не запущен

**Решение:**
```bash
# Проверьте логи backend
sudo docker logs -f backend

# Проверьте конфигурацию nginx
sudo docker exec nginx cat /etc/nginx/conf.d/default.conf

# Попробуйте напрямую (если порт открыт)
curl http://194.67.205.158:8000/docs
```

### Проблема: Метрики не записываются в БД

**Возможные причины:**
- Endpoint `/api/behavior-metrics/` возвращает 404 или 422
- Проблемы с валидацией данных

**Решение:**
```bash
# Проверьте логи backend
sudo docker logs -f backend | grep behavior

# Проверьте в браузере (DevTools → Network)
# Найдите POST запросы на /api/behavior-metrics/
# Посмотрите статус и тело ошибки

# Проверьте таблицу в БД
sudo docker exec -it postgres psql -U orderuser -d orderdb
SELECT * FROM behavior_metrics ORDER BY created_at DESC LIMIT 10;
```

### Проблема: Фронтенд не обновляется

**Решение:**
```bash
# Пересоберите фронтенд
cd frontend
npm run build

# Перезапустите nginx
sudo docker compose restart nginx

# Очистите кэш браузера (Ctrl+Shift+Del)
```

### Проблема: JWT авторизация не работает

**Возможные причины:**
- Неправильный `SECRET_KEY`
- Токен не передается в заголовке
- Токен истек

**Решение:**
```bash
# Проверьте .env
cat .env | grep SECRET_KEY

# Проверьте формат в браузере (DevTools → Network → Request Headers)
# Должно быть: Authorization: Bearer <длинная строка>

# Проверьте срок действия токена в backend логах
sudo docker logs -f backend | grep token
```

---

## 📚 Дополнительные ресурсы

- **FastAPI документация:** https://fastapi.tiangolo.com/
- **SQLAlchemy:** https://www.sqlalchemy.org/
- **Docker Compose:** https://docs.docker.com/compose/
- **Nginx:** https://nginx.org/en/docs/
- **JWT:** https://jwt.io/

---

## 📝 Changelog

### v1.0.0 (урок 3)
- ✅ JWT авторизация админ-панели
- ✅ CRUD операции для услуг через визуальную таблицу
- ✅ Система сбора поведенческих метрик (время, клики, heatmap)
- ✅ Интеллектуальная приоритизация заявок по "температуре"
- ✅ Безопасность: закрытие портов backend и PostgreSQL
- ✅ Production-ready конфигурация с HTTPS

### v0.2.0 (урок 2)
- ✅ Backend API (FastAPI)
- ✅ База данных (PostgreSQL)
- ✅ Форма заявки на главной странице
- ✅ Базовая админ-панель

### v0.1.0 (урок 1)
- ✅ Проектирование архитектуры
- ✅ Docker Compose настройка

---

## 👥 Авторы

Учебный проект в рамках курса "Вайб-кодинг (Python)".

**Репозиторий:** https://github.com/ergon73/order-processing-vps-infra  
**VPS:** service.prompt-engineer.su (194.67.205.158)

---

## 📄 Лицензия

Учебный проект. Свободное использование в образовательных целях.

---

## 🤝 Содействие

Если вы нашли баг или хотите предложить улучшение:

1. Создайте Issue в GitHub
2. Опишите проблему или идею
3. Приложите скриншоты/логи (если применимо)

---

**Успешной разработки! 🚀**
