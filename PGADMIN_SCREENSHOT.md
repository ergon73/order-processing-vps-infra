# 📸 Инструкция: Как сделать скриншот таблицы applications в pgAdmin

## Шаги для просмотра заявок

### 1. Откройте pgAdmin в браузере
```
http://194.67.205.158:5050
```

### 2. Войдите в pgAdmin
- **Email**: `admin@example.com`
- **Password**: из файла `.env` (переменная `PGADMIN_PASSWORD`)

### 3. Подключитесь к PostgreSQL серверу

**Если сервер еще не добавлен:**
1. В левом меню найдите **"Servers"**
2. Правый клик → **"Register"** → **"Server"**
3. Во вкладке **"General"**:
   - **Name**: `postgres` (или любое имя)
4. Во вкладке **"Connection"**:
   - **Host name/address**: `postgres` (или IP сервера `194.67.205.158`)
   - **Port**: `5432`
   - **Maintenance database**: `app_db`
   - **Username**: `app_user`
   - **Password**: из файла `.env` (переменная `POSTGRES_PASSWORD`)
5. Нажмите **"Save"**

**Если сервер уже добавлен:**
- Просто разверните его в левом меню (кликните на стрелку слева от имени сервера)

### 4. Найдите базу данных `app_db`

В дереве слева:
```
Servers
  └── postgres (или ваше имя сервера)
      └── Databases
          └── app_db  ← кликните здесь
```

### 5. Найдите таблицу `applications`

В дереве слева:
```
app_db
  └── Schemas
      └── public
          └── Tables
              └── applications  ← кликните ПРАВОЙ кнопкой мыши
```

### 6. Откройте данные таблицы

**Способ 1 (через контекстное меню):**
1. Правый клик на `applications`
2. Выберите **"View/Edit Data"** → **"All Rows"**
3. Или используйте **"View/Edit Data"** → **"First 100 Rows"**

**Способ 2 (через SQL Query Tool):**
1. Правый клик на `applications`
2. Выберите **"Query Tool"**
3. Введите SQL:
   ```sql
   SELECT * FROM applications ORDER BY id DESC;
   ```
4. Нажмите **"Execute"** (F5) или кнопку ▶️

### 7. Сделайте скриншот

**Что должно быть видно на скриншоте:**
- ✅ Заголовки столбцов (id, first_name, last_name, interested_product, created_at, и т.д.)
- ✅ Строки с данными заявок (минимум 1-2 строки с вашими тестовыми данными)
- ✅ Видно поле `interested_product` с выбранной услугой (например, "Полировка кузова премиум-класса")

**Рекомендации для скриншота:**
- Покажите минимум 2-3 столбца: `id`, `first_name`, `last_name`, `interested_product`, `created_at`
- Можно скроллить таблицу, если строк много
- Включите заголовки таблицы для читаемости

---

## Альтернативный способ: через SQL Query Tool

### Шаг 1: Откройте Query Tool
1. Правый клик на базе `app_db` → **"Query Tool"**
   ИЛИ
2. Правый клик на таблице `applications` → **"Query Tool"**

### Шаг 2: Выполните запрос
```sql
SELECT 
    id, 
    first_name, 
    last_name, 
    interested_product, 
    budget,
    created_at 
FROM applications 
ORDER BY id DESC;
```

### Шаг 3: Нажмите Execute (F5)

### Шаг 4: Сделайте скриншот результата

---

## Ожидаемые данные в таблице

После всех тестов в таблице `applications` должны быть записи:
- ID 1: Иван Иванов - "Полировка кузова премиум-класса"
- ID 2: Георгий Белянин - "Полировка кузова премиум-класса"
- ID 3: Георгий Белянин - "Химчистка салона с озонированием"
- ID 4: Георгий Белянин - "Нанесение керамического покрытия"

---

## Быстрая проверка через командную строку (без pgAdmin)

Если нужно быстро проверить данные:

```bash
cd /root/order-processing-vps-infra
docker compose exec postgres psql -U app_user -d app_db -c "SELECT id, first_name, last_name, interested_product, created_at FROM applications ORDER BY id DESC;"
```

Но для скриншота в сдачу нужен именно pgAdmin интерфейс!
