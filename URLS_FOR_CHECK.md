# 🌐 URL для визуальной проверки

**IP сервера:** `194.67.205.158`  
**Домен:** `service.prompt-engineer.su` (если настроен HTTPS)

---

## ✅ Part 3 - Новые обязательные проверки

### 1. **Страница авторизации**
```
http://194.67.205.158/login.html
или
https://service.prompt-engineer.su/login.html (если HTTPS настроен)
```

**Что проверить:**
- ✅ Страница загружается без ошибок
- ✅ CSS стили применяются
- ✅ При первом посещении (если админов нет) видна **форма регистрации**
- ✅ После регистрации первого админа форма регистрации **исчезает**
- ✅ Форма входа работает

**Тест регистрации первого админа:**
1. Откройте `/login.html`
2. Если админов нет - должна быть видна форма регистрации
3. Заполните:
   - Email: `admin@example.com`
   - Пароль: `StrongPassword123`
   - Подтвердите пароль: `StrongPassword123`
4. Нажмите "Зарегистрироваться"
5. ✅ Должно появиться сообщение "Регистрация успешна!"
6. ✅ Форма регистрации должна **исчезнуть**
7. Обновите страницу - формы регистрации **нет**

**Примечание:** Если админ уже зарегистрирован (проверьте через `GET /api/auth/check`), форма регистрации будет скрыта автоматически. Для тестирования регистрации можно удалить админа из БД через pgAdmin или использовать другой email.

**Тест входа:**
1. Заполните форму входа:
   - Email: `admin@example.com`
   - Пароль: `StrongPassword123`
2. Нажмите "Войти"
3. ✅ Должно появиться сообщение "Вход успешен! Перенаправление..."
4. ✅ Автоматическое перенаправление на `/admin.html`

---

### 2. **Админ-панель**
```
http://194.67.205.158/admin.html
или
https://service.prompt-engineer.su/admin.html (если HTTPS настроен)
```

**Что проверить:**
- ✅ Страница загружается (требует JWT авторизацию)
- ✅ Если не авторизован - перенаправление на `/login.html`
- ✅ Три вкладки: "Услуги", "Заявки", "Статистика"

**Вкладка "Услуги" - CRUD операции:**
1. ✅ Таблица услуг загружается автоматически
2. Кнопка "Добавить услугу" открывает модальное окно
3. Форма создания:
   - Название: `Тестовая услуга`
   - Бюджет: `50k-100k`
   - Нажмите "Сохранить"
   - ✅ Услуга должна появиться в таблице
4. Кнопка "Редактировать" открывает форму редактирования
   - Измените название на `Обновленная услуга`
   - ✅ Изменения сохраняются
5. Кнопка "Удалить" удаляет услугу
   - ✅ Услуга исчезает из таблицы
   - ✅ Услуга исчезает из dropdown на главной странице

**Вкладка "Заявки":**
1. ✅ Таблица заявок загружается автоматически
2. ✅ Заявки отсортированы по приоритету (score DESC)
3. Цветовые индикаторы:
   - 🔥 **Красный** (score > 80) - высокий приоритет
   - ⚠️ **Желтый** (score 50-80) - средний приоритет
   - ✅ **Зеленый** (score < 50) - низкий приоритет
4. Кнопка "Просмотр" открывает модальное окно с деталями заявки
   - ✅ Отображаются все поля: имя, услуга, бюджет, приоритет и т.д.

**Вкладка "Статистика":**
1. ✅ Отображается среднее время на странице (день/неделя/месяц)
2. ✅ Отображается heatmap координат курсора (Canvas)
3. После активности на главной странице 2-3 минуты:
   - ✅ Статистика обновляется
   - ✅ Heatmap показывает популярные зоны

---

### 3. **API - Авторизация (JWT)**
```
http://194.67.205.158/api/docs
```

**Endpoints для проверки:**

#### `GET /api/auth/check`
- ✅ Возвращает `{"admin_exists": true/false}`
- ✅ Публичный endpoint (не требует JWT)

#### `POST /api/auth/register`
1. В Swagger UI нажмите "Try it out"
2. Заполните:
   ```json
   {
     "email": "admin@test.com",
     "password": "TestPassword123"
   }
   ```
3. ✅ Первая регистрация возвращает **201 Created** (если админов нет)
4. ✅ Повторная регистрация возвращает **403 Forbidden** (если админы уже есть)
5. ⚠️ **Важно:** Регистрация разрешена **только если админов нет**. После создания первого админа все последующие попытки регистрации будут отклонены.

#### `POST /api/auth/login`
1. Используйте OAuth2PasswordRequestForm (form-data):
   - `username`: `admin@example.com` (OAuth2 использует `username`, не `email`)
   - `password`: `StrongPassword123`
2. ✅ Возвращает `{"access_token": "...", "token_type": "bearer"}`
3. Скопируйте токен
4. Нажмите "Authorize" в Swagger UI
5. Введите: `Bearer <your_token>`
6. Нажмите "Authorize"

#### `GET /api/auth/me` (защищенный)
1. После авторизации в Swagger UI
2. ✅ Возвращает профиль админа (id, email, created_at)
3. ✅ Без токена возвращает **401 Unauthorized**

---

### 4. **API - CRUD услуг (JWT защищенные)**
```
http://194.67.205.158/api/docs
```

**Обновленные endpoints:**

#### `POST /api/admin-settings/` (требует JWT)
1. Авторизуйтесь в Swagger UI (Bearer token)
2. Создайте услугу:
   ```json
   {
     "services": "Новая услуга",
     "budget_range": "100k-200k"
   }
   ```
3. ✅ Возвращает **201 Created**

#### `PUT /api/admin-settings/{id}` (требует JWT)
1. Авторизуйтесь в Swagger UI
2. Укажите `id` существующей услуги
3. Обновите данные:
   ```json
   {
     "services": "Обновленная услуга",
     "budget_range": "150k-250k"
   }
   ```
4. ✅ Возвращает **200 OK** с обновленными данными

#### `DELETE /api/admin-settings/{id}` (требует JWT)
1. Авторизуйтесь в Swagger UI
2. Укажите `id` услуги для удаления
3. ✅ Возвращает **204 No Content**
4. ✅ Услуга исчезает из `GET /api/admin-settings/`

#### `GET /api/admin-settings/` (публичный)
- ✅ Возвращает список всех услуг (не требует JWT)
- ✅ Используется главной страницей для dropdown

---

### 5. **API - Заявки с приоритизацией**
```
http://194.67.205.158/api/docs
```

#### `GET /api/applications/` (требует JWT)
1. Авторизуйтесь в Swagger UI
2. ✅ Возвращает список заявок
3. ✅ Заявки отсортированы по `priority_score` (DESC)
4. ✅ Каждая заявка содержит поле `priority_score` (0-100)
5. Без токена возвращает **401 Unauthorized**

#### `POST /api/applications/` (публичный)
- ✅ Не требует JWT (публичная форма)
- ✅ При создании автоматически вычисляется `priority_score`

#### `GET /api/applications/{id}` (требует JWT)
1. Авторизуйтесь в Swagger UI
2. Укажите `id` заявки
3. ✅ Возвращает детали заявки
4. ✅ Содержит все поля, включая `priority_score`

---

### 6. **API - Поведенческие метрики**
```
http://194.67.205.158/api/docs
```

#### `POST /api/behavior-metrics/` (публичный)
1. В Swagger UI создайте запрос:
   ```json
   {
     "application_id": 0,
     "time_on_page": 60,
     "buttons_clicked": "{\"submit-button\": 1}",
     "cursor_positions": "[{\"x\": 100, \"y\": 200}, {\"x\": 150, \"y\": 250}]",
     "return_frequency": 0
   }
   ```
2. ✅ Возвращает **201 Created**
3. ✅ `application_id` может быть `0` или `null` (для анонимных метрик)

#### `GET /api/behavior-metrics/stats` (требует JWT)
1. Авторизуйтесь в Swagger UI
2. ✅ Возвращает агрегированную статистику:
   ```json
   {
     "average_time_on_page": {
       "day": 45.5,
       "week": 42.3,
       "month": 38.7
     },
     "heatmap_coordinates": [...]
   }
   ```
3. После активности на главной странице 2-3 минуты:
   - ✅ Статистика обновляется
   - ✅ `heatmap_coordinates` содержит координаты курсора

---

## ✅ Part 2 - Базовые проверки (сохраняются)

### 1. **Главная страница (Frontend)**
```
http://194.67.205.158/
```

**Что проверить:**
- ✅ Страница загружается без ошибок
- ✅ CSS стили применяются (белая тема, шрифт Playfair Display)
- ✅ Видны анимации (floating background)
- ✅ Форма заявки отображается корректно
- ✅ В DevTools → Network **нет 404** на `main.*.css` и `main.*.js`
- ✅ Dropdown "Выберите услугу" загружается из API
- ✅ **Метрики собираются автоматически** (DevTools → Network показывает `POST /api/behavior-metrics/` каждую секунду)

---

### 2. **Swagger UI (API документация)**
```
http://194.67.205.158/api/docs
```

**Что проверить:**
- ✅ Swagger UI открывается и загружается
- ✅ Видны **все** эндпоинты:
  - `/auth/check` - Check admin exists
  - `/auth/register` - Register first admin
  - `/auth/login` - Login (OAuth2)
  - `/auth/me` - Get current admin (JWT protected)
  - `/admin-settings/` - CRUD услуг (PUT, DELETE требуют JWT)
  - `/applications/` - Список заявок (GET требует JWT, POST публичный)
  - `/behavior-metrics/` - Метрики (POST публичный, GET /stats требует JWT)

---

### 3. **CSS файл (проверка статики)**
```
http://194.67.205.158/main.*.css
```

**Что проверить:**
- ✅ Файл загружается (HTTP 200, не 404)
- ✅ Содержит CSS стили

---

### 4. **API Endpoint - Список услуг (публичный)**
```
http://194.67.205.158/api/admin-settings/
```

**Что проверить:**
- ✅ Возвращает JSON с массивом услуг
- ✅ Каждая услуга содержит: `id`, `services`, `budget_range`, `created_at`, `updated_at`

---

## 🧪 Функциональное тестирование Part 3

### Тест полного цикла авторизации и CRUD:

1. **Регистрация первого админа:**
   - Откройте `/login.html`
   - Зарегистрируйтесь
   - ✅ Форма регистрации исчезает

2. **Вход в систему:**
   - Войдите с созданными данными
   - ✅ Перенаправление на `/admin.html`

3. **CRUD услуг в админ-панели:**
   - Создайте 5 услуг через интерфейс
   - ✅ Услуги появляются в таблице
   - Отредактируйте одну услугу
   - ✅ Изменения сохраняются
   - Удалите одну услугу
   - ✅ Услуга исчезает

4. **Проверка главной страницы:**
   - Откройте `/`
   - ✅ Dropdown содержит все созданные услуги (кроме удаленной)

5. **Сбор метрик:**
   - На главной странице проведите 2-3 минуты
   - Перемещайте курсор, нажимайте кнопки
   - ✅ В DevTools → Network видны запросы `POST /api/behavior-metrics/` каждую секунду

6. **Просмотр статистики:**
   - В админ-панели перейдите на вкладку "Статистика"
   - ✅ Видно среднее время на странице
   - ✅ Heatmap отображает координаты курсора

7. **Приоритизация заявок:**
   - Выполните SQL скрипт `scripts/seed_applications.sql` в pgAdmin
   - В админ-панели откройте вкладку "Заявки"
   - ✅ Заявки отсортированы по приоритету (4 срочные → 3 средние → 3 низкие)
   - ✅ Цветовые индикаторы соответствуют приоритету

---

## 🔍 Дополнительные проверки

### pgAdmin (проверка данных в БД)
```
http://194.67.205.158:5050
```

**Что проверить:**
1. Подключиться к PostgreSQL серверу `postgres`
2. Открыть базу `app_db`
3. **Таблица `admins`:**
   - ✅ Должна содержать зарегистрированных админов
   - ✅ Пароли захешированы (bcrypt, поле `hashed_password`)
4. **Таблица `admin_settings`:**
   - ✅ Должна содержать созданные услуги
5. **Таблица `applications`:**
   - ✅ Должна содержать отправленные заявки
   - ✅ Поле `priority_score` заполнено (0-100)
6. **Таблица `behavior_metrics`:**
   - ✅ Должна содержать записи метрик
   - ✅ `application_id` может быть `0` или `NULL`
   - ✅ `cursor_positions` содержит JSON строку с координатами

---

## ⚠️ Типичные проблемы и их симптомы

### Проблема: 401 Unauthorized на защищенных endpoints
**Симптом:** `GET /api/applications/` возвращает 401  
**Причина:** Не передан JWT токен или токен истек  
**Решение:** Авторизуйтесь через `/auth/login`, скопируйте токен, используйте в Swagger UI "Authorize"

### Проблема: Форма регистрации не исчезает
**Симптом:** После регистрации форма все еще видна  
**Причина:** JavaScript не обновил DOM или ошибка в `auth.js`  
**Решение:** 
- Проверьте `/api/auth/check` - должен возвращать `{"admin_exists": true}`
- Откройте DevTools → Console и проверьте ошибки JavaScript
- Убедитесь, что `auth.js` загружается (Network → `auth.js` → 200)
- Обновите страницу с очисткой кэша (Ctrl+F5)

### Проблема: Ошибка регистрации "Internal Server Error" или "Ошибка регистрации"
**Симптом:** При регистрации появляется "Ошибка регистрации" в красном цвете  
**Причина:** Проблема с хешированием пароля (bcrypt) - уже исправлено  
**Решение:** 
- Используется прямой `bcrypt` вместо `passlib` (исправлено в коде)
- Если ошибка повторяется, проверьте логи backend: `docker logs backend | grep -i error`
- Убедитесь, что backend пересобран после изменений: `docker compose up -d --build backend`

### Проблема: Метрики не отправляются
**Симптом:** В DevTools → Network нет запросов к `/api/behavior-metrics/`  
**Причина:** Скрипт `behavior-metrics.js` не загружается  
**Решение:** Проверьте, что `index.html` содержит `<script src="./behavior-metrics.js"></script>`

### Проблема: Heatmap пустой
**Симптом:** В админ-панели heatmap не отображает данные  
**Причина:** Нет метрик в БД или координаты не парсятся  
**Решение:** Проверьте таблицу `behavior_metrics`, убедитесь что `cursor_positions` содержит валидный JSON

### Проблема: Заявки не сортируются по приоритету
**Симптом:** В админ-панели заявки в случайном порядке  
**Причина:** `priority_score` не вычисляется или не сохраняется  
**Решение:** Проверьте функцию `calculate_priority_score()` в `core/priority.py`, проверьте поле `priority_score` в модели `Application`

---

## 📋 Чек-лист для сдачи Part 3

### Авторизация:
- [ ] Страница `/login.html` загружается
- [ ] Форма регистрации показывается только если админов нет
- [ ] После регистрации форма регистрации исчезает
- [ ] Вход в систему работает и перенаправляет на `/admin.html`
- [ ] `GET /api/auth/check` возвращает `{"admin_exists": true/false}`
- [ ] `POST /api/auth/login` выдает JWT токен
- [ ] `GET /api/auth/me` требует JWT и возвращает профиль админа

### Админ-панель:
- [ ] `/admin.html` требует авторизацию (перенаправление на `/login.html` если нет токена)
- [ ] Вкладка "Услуги": таблица загружается, CRUD операции работают
- [ ] Вкладка "Заявки": список загружается, сортировка по приоритету работает
- [ ] Вкладка "Статистика": среднее время на странице и heatmap отображаются

### CRUD услуг:
- [ ] `POST /api/admin-settings/` требует JWT
- [ ] `PUT /api/admin-settings/{id}` требует JWT
- [ ] `DELETE /api/admin-settings/{id}` требует JWT
- [ ] `GET /api/admin-settings/` публичный (используется главной страницей)

### Метрики:
- [ ] `POST /api/behavior-metrics/` принимает метрики (публичный)
- [ ] `GET /api/behavior-metrics/stats` возвращает статистику (требует JWT)
- [ ] Метрики собираются на главной странице автоматически
- [ ] Heatmap отображается в админ-панели

### Приоритизация:
- [ ] `GET /api/applications/` требует JWT и возвращает отсортированные заявки
- [ ] SQL скрипт `scripts/seed_applications.sql` создает 10 заявок
- [ ] Заявки имеют поле `priority_score` (0-100)
- [ ] Цветовые индикаторы в админ-панели соответствуют приоритету

### Безопасность (если реализовано):
- [ ] Порты 8000 и 5432 закрыты (403 или недоступны)
- [ ] HTTPS работает (если настроен Let's Encrypt)
- [ ] pgAdmin и registry отключены в prod (если применено)

---

**Все URL готовы к проверке! Откройте их в браузере и проверьте каждый пункт.**
