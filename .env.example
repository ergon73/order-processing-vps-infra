# PostgreSQL настройки
POSTGRES_DB=app_db
POSTGRES_USER=app_user
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD_12_CHARS_MIN

# pgAdmin настройки
PGADMIN_EMAIL=admin@example.com
PGADMIN_PASSWORD=CHANGE_ME_STRONG_PASSWORD_12_CHARS_MIN

# FastAPI JWT настройки
SECRET_KEY=CHANGE_ME_GENERATE_WITH_openssl_rand_hex_32

# Docker Registry URL (для будущего использования)
# REGISTRY_URL=YOUR_VPS_IP:5000

# Примечания:
# 1. Скопируйте этот файл в .env: cp .env.example .env
# 2. Замените все CHANGE_ME_* на сильные пароли (минимум 12 символов)
# 3. Для SECRET_KEY используйте: openssl rand -hex 32
# 4. Используйте разные пароли для PostgreSQL и pgAdmin
# 5. Файл .env НЕ должен попадать в git (уже добавлен в .gitignore)
