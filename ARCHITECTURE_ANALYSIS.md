# Анализ архитектурных расхождений проекта

**Дата проверки:** 2025-01-18  
**Сравнение:** Текущая структура проекта vs `genai-readme.md` (инструкции для Part 3)

---

## 🔴 КРИТИЧЕСКИЕ РАСХОЖДЕНИЯ

### 1. Структура Backend

**Ожидается в genai-readme.md:**
```
backend/
├── app/
│   ├── main.py
│   ├── database.py
│   ├── models.py
│   ├── schemas.py
│   └── routers/
│       ├── auth.py          # ❌ ОТСУТСТВУЕТ
│       ├── admin_settings.py
│       ├── applications.py
│       └── behavior_metrics.py  # ❌ ОТСУТСТВУЕТ
```

**Реальная структура:**
```
backend/
├── main.py                  # ✅ Есть (но не в app/)
├── core/
│   └── database.py         # ✅ Есть (но не в app/)
├── models/                 # ✅ Есть (но не models.py, а папка)
│   ├── admin_settings.py
│   ├── applications.py
│   └── behavior_metrics.py
└── routes/                 # ✅ Есть (но не routers/)
    ├── admin_settings.py
    └── applications.py
    # ❌ НЕТ auth.py
    # ❌ НЕТ routes/behavior_metrics.py
```

**Проблемы:**
- ❌ **Нет папки `app/`** — структура плоская
- ❌ **Нет `routers/auth.py`** — JWT авторизация не реализована
- ❌ **Нет `routers/behavior_metrics.py`** — метрики не реализованы
- ❌ **Нет `schemas.py`** — Pydantic схемы могут быть разбросаны по файлам
- ⚠️ **Структура отличается** — но функционально может работать

---

### 2. Структура Frontend

**Ожидается в genai-readme.md:**
```
frontend/src/
├── index.html              # ✅ Есть
├── admin.html              # ❌ ОТСУТСТВУЕТ
├── login.html              # ❌ ОТСУТСТВУЕТ
├── js/
│   ├── admin.js            # ❌ ОТСУТСТВУЕТ
│   ├── behavior-metrics.js # ❌ ОТСУТСТВУЕТ
│   └── auth.js             # ❌ ОТСУТСТВУЕТ
└── css/
    └── styles.css          # ✅ Есть (но в корне src/)
```

**Реальная структура:**
```
frontend/src/
├── index.html              # ✅ Есть
├── index.js                # ✅ Есть (но не в js/)
└── styles.css              # ✅ Есть (но не в css/)
```

**Проблемы:**
- ❌ **Нет `admin.html`** — админ-панель не реализована
- ❌ **Нет `login.html`** — страница авторизации не реализована
- ❌ **Нет папки `js/`** — JavaScript файлы в корне `src/`
- ❌ **Нет `behavior-metrics.js`** — сбор метрик не реализован
- ❌ **Нет `auth.js`** — JWT авторизация на фронтенде не реализована

---

### 3. Backend - Отсутствующие компоненты

#### 3.1 JWT Авторизация
- ❌ **Нет модели `admins`** в `models/`
- ❌ **Нет роута `routes/auth.py`** с endpoints:
  - `GET /api/auth/check`
  - `POST /api/auth/register`
  - `POST /api/auth/login`
  - `GET /api/auth/me`
- ❌ **Нет зависимостей для JWT** в `requirements.txt`:
  - `python-jose[cryptography]` или `PyJWT`
  - `passlib[bcrypt]` для хеширования паролей

#### 3.2 Поведенческие метрики
- ✅ **Модель `behavior_metrics.py` есть** в `models/`
- ❌ **Нет роута `routes/behavior_metrics.py`** с endpoints:
  - `POST /api/behavior-metrics/`
  - `GET /api/behavior-metrics/stats`

#### 3.3 Приоритизация заявок
- ❌ **Нет функции `calculate_priority_score()`** в backend
- ❌ **Нет сортировки по score** в `GET /api/applications`

---

### 4. Frontend - Отсутствующие компоненты

#### 4.1 Админ-панель
- ❌ **Нет `admin.html`** — главная страница админки
- ❌ **Нет `admin.js`** — логика CRUD услуг, просмотр заявок
- ❌ **Нет таблицы услуг** с кнопками редактирования/удаления
- ❌ **Нет списка заявок** с приоритизацией
- ❌ **Нет раздела "Статистика пользователей"** с heatmap

#### 4.2 Авторизация
- ❌ **Нет `login.html`** — страница входа
- ❌ **Нет `auth.js`** — логика JWT (login, register, check)
- ❌ **Нет проверки `GET /api/auth/check`** при загрузке админки

#### 4.3 Метрики
- ❌ **Нет `behavior-metrics.js`** — сбор метрик на главной странице
- ❌ **Нет подключения скрипта** в `index.html`

---

### 5. Docker Compose - Конфигурация

**Ожидается в genai-readme.md (для prod):**
```yaml
backend:
  # НЕТ секции ports: наружу!
  expose:
    - "8000"
  environment:
    - DATABASE_URL=${DATABASE_URL}
    - SECRET_KEY=${SECRET_KEY}

postgres:
  # НЕТ секции ports: наружу!

pgadmin:
  profiles: ["dev"]  # Отключен в prod

registry:
  profiles: ["dev"]  # Отключен в prod
```

**Реальная конфигурация:**
```yaml
backend:
  # ✅ НЕТ ports (правильно)
  # ❌ НЕТ SECRET_KEY в environment
  # ❌ НЕТ DATABASE_URL в environment
  environment:
    - POSTGRES_HOST: postgres
    - POSTGRES_PORT: 5432
    - POSTGRES_USER: ${POSTGRES_USER}
    - POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    - POSTGRES_DB: ${POSTGRES_DB}

postgres:
  # ✅ НЕТ ports (правильно)

pgadmin:
  # ❌ АКТИВЕН (нет profiles: ["dev"])

registry:
  # ❌ АКТИВЕН (нет profiles: ["dev"])
```

**Проблемы:**
- ❌ **Нет `SECRET_KEY`** в environment backend (нужен для JWT)
- ❌ **Нет `DATABASE_URL`** (опционально, но может быть полезно)
- ❌ **pgAdmin и registry активны** — должны быть в профиле dev для prod

---

### 6. Nginx - Конфигурация

**Ожидается в genai-readme.md:**
```nginx
# Редирект с HTTP на HTTPS
server {
    listen 80;
    return 301 https://$host$request_uri;
}

# Основной сервер HTTPS
server {
    listen 443 ssl;
    ssl_certificate /etc/nginx/ssl/cert.pem;
    ssl_certificate_key /etc/nginx/ssl/key.pem;
    # ...
}
```

**Реальная конфигурация:**
```nginx
server {
    listen 80;  # ❌ Только HTTP, нет HTTPS
    # ❌ НЕТ SSL конфигурации
    # ❌ НЕТ редиректа на HTTPS
}
```

**Проблемы:**
- ❌ **Нет HTTPS (443)** — только HTTP (80)
- ❌ **Нет SSL сертификатов** — папка `ssl/` отсутствует
- ❌ **Нет редиректа HTTP → HTTPS**

---

### 7. Отсутствующие файлы

- ❌ **`scripts/seed_applications.sql`** — SQL скрипт для тестовых заявок
- ❌ **`ssl/cert.pem`** и **`ssl/key.pem`** — SSL сертификаты
- ❌ **`.env.example`** — пример переменных окружения (есть, но не читается из-за gitignore)

---

## 🟡 НЕКРИТИЧЕСКИЕ РАСХОЖДЕНИЯ

### 1. Структура Backend (работает, но отличается)

**Текущая структура функциональна**, но не соответствует ожиданиям в genai-readme.md:
- ✅ `core/database.py` вместо `app/database.py` — работает
- ✅ `models/` (папка) вместо `models.py` (файл) — лучше для масштабирования
- ✅ `routes/` вместо `routers/` — просто другое название

**Рекомендация:** Можно оставить, но нужно добавить недостающие файлы.

---

### 2. Requirements.txt

**Ожидается:**
- `python-jose[cryptography]` или `PyJWT` — для JWT
- `passlib[bcrypt]` — для хеширования паролей

**Реальная:**
```txt
fastapi
uvicorn[standard]
sqlalchemy
psycopg2-binary
pydantic
pydantic-settings
```

**Проблема:** Нет зависимостей для JWT и хеширования паролей.

---

## ✅ ЧТО РАБОТАЕТ ПРАВИЛЬНО

1. ✅ **Docker Compose** — backend и postgres без внешних портов
2. ✅ **Nginx** — правильно проксирует `/api/` на backend
3. ✅ **Модели БД** — `admin_settings`, `applications`, `behavior_metrics` существуют
4. ✅ **Базовые роуты** — `admin_settings`, `applications` работают
5. ✅ **Frontend структура** — Webpack настроен, сборка работает
6. ✅ **Главная страница** — форма заявки работает

---

## 📋 ПЛАН ДОРАБОТКИ (по приоритету)

### Приоритет 1: JWT Авторизация
1. Добавить зависимости в `requirements.txt`:
   - `python-jose[cryptography]`
   - `passlib[bcrypt]`
2. Создать модель `models/admins.py`
3. Создать роут `routes/auth.py` с 4 endpoints
4. Создать `frontend/src/login.html` и `frontend/src/auth.js`
5. Добавить `SECRET_KEY` в `docker-compose.yml`

### Приоритет 2: Админ-панель
1. Создать `frontend/src/admin.html`
2. Создать `frontend/src/admin.js` с CRUD услуг
3. Добавить таблицу заявок с приоритизацией
4. Добавить раздел статистики с heatmap

### Приоритет 3: Метрики
1. Создать `frontend/src/behavior-metrics.js`
2. Подключить в `index.html`
3. Создать роут `routes/behavior_metrics.py`
4. Добавить endpoint `GET /api/behavior-metrics/stats`

### Приоритет 4: Приоритизация
1. Добавить функцию `calculate_priority_score()` в backend
2. Обновить `GET /api/applications` — сортировка по score
3. Создать `scripts/seed_applications.sql`

### Приоритет 5: Безопасность (Prod)
1. Сгенерировать SSL сертификаты
2. Настроить HTTPS в nginx
3. Добавить редирект HTTP → HTTPS
4. Переместить pgadmin и registry в профиль dev

---

## 🎯 ВЫВОДЫ

**Текущее состояние:** Проект на стадии Part 2 (базовая функциональность работает)

**Для Part 3 нужно добавить:**
- ❌ JWT авторизация (backend + frontend)
- ❌ Админ-панель (CRUD услуг, просмотр заявок)
- ❌ Поведенческие метрики (сбор + отображение)
- ❌ Приоритизация заявок (алгоритм + визуализация)
- ❌ HTTPS и безопасность (SSL, закрытие портов)

**Архитектурные расхождения:**
- Структура backend отличается (но функциональна)
- Структура frontend упрощена (нет папок js/css)
- Отсутствуют критические компоненты для Part 3

**Рекомендация:** Можно дорабатывать по genai-readme.md, но нужно учесть текущую структуру и адаптировать инструкции.
