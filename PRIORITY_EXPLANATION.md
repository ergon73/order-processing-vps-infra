# 🎯 Как работает приоритизация заявок

## Автоматическая приоритизация

Приоритизация происходит **автоматически** в двух случаях:

### 1. При создании заявки (POST /api/applications/)

Когда пользователь отправляет форму на главной странице:
- Автоматически вызывается функция `calculate_priority_score()`
- Score вычисляется на основе:
  - **Бюджет (40%)**: 5M+ → 40, 1-5M → 30, 500k-1M → 20, <500k → 10
  - **Размер компании (30%)**: 500+ → 30, 100-500 → 20, 50-100 → 15, <50 → 5
  - **Срочность (30%)**: "неделя"/"срочно" → 30, "месяц" → 15, иначе → 5
- Score сохраняется в поле `priority_score` в БД

### 2. При запросе списка заявок (GET /api/applications/)

Когда админ открывает вкладку "Заявки":
- Для каждой заявки пересчитывается score (на случай если алгоритм изменился)
- Если score изменился - автоматически обновляется в БД
- Заявки сортируются по score (от большего к меньшему)

## Почему текущие заявки имеют одинаковый приоритет (20)?

Текущие заявки имеют приоритет 20, потому что:
- **Бюджет**: небольшие суммы (20k-300k) → получают 10 баллов
- **Размер компании**: поле пустое (NULL) → получают 5 баллов
- **Срочность**: поле deadline пустое (NULL) → получают 5 баллов
- **Итого**: 10 + 5 + 5 = 20 баллов

## Как проверить приоритизацию?

### Вариант 1: Создать новую заявку с высоким приоритетом

1. Откройте главную страницу: `http://194.67.205.158/`
2. Заполните форму:
   - Услуга: любая
   - Имя: `Тест`
   - Фамилия: `Высокий приоритет`
   - Бюджет: `5000000-10000000` (5-10 миллионов)
   - Размер компании: `500+`
   - Срок: `1 неделя`
3. Отправьте заявку
4. В админ-панели → "Заявки":
   - ✅ Новая заявка должна быть вверху списка
   - ✅ Приоритет должен быть 🔥 (красный, score > 80)

### Вариант 2: Выполнить SQL скрипт с тестовыми данными

1. Откройте pgAdmin: `http://194.67.205.158:5050`
2. Подключитесь к базе `app_db`
3. Выполните SQL скрипт: `scripts/seed_applications.sql`
4. ✅ Создастся 10 заявок:
   - 4 срочные (score > 80) - 🔥 красные
   - 3 средние (score 50-80) - ⚠️ желтые
   - 3 низкие (score < 50) - ✅ зеленые
5. В админ-панели → "Заявки":
   - ✅ Заявки отсортированы: 4 срочные → 3 средние → 3 низкие

## Формула приоритизации

```
priority_score = бюджет_score + company_size_score + urgency_score

Где:
- бюджет_score: 10-40 (зависит от суммы)
- company_size_score: 5-30 (зависит от размера компании)
- urgency_score: 5-30 (зависит от срочности)

Максимальный score: 100 (40 + 30 + 30)
Минимальный score: 20 (10 + 5 + 5)
```

## Цветовые индикаторы

- 🔥 **Красный** (score > 80) - Высокий приоритет (Urgent)
- ⚠️ **Желтый** (score 50-80) - Средний приоритет
- ✅ **Зеленый** (score < 50) - Низкий приоритет
